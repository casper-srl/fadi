{
  "name": "fadi-vercel-root",
  "description": "Bridge di build per Vercel: compila il progetto Astro in Fadi/ e copia l'output nella struttura attesa da Vercel.",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "build": "npm --prefix Fadi ci && npm --prefix Fadi run build && node tools/copy-vercel-output.mjs"
  }
}
